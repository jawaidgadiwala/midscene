{
  "name": "fork-docs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "sync": "node ./scripts/sync-docs.mjs",
    "dev": "pnpm run sync && rspress dev",
    "build": "pnpm run sync && rspress build",
    "preview": "rspress preview"
  },
  "devDependencies": {
    "@rspress/core": "2.0.18"
  }
}
